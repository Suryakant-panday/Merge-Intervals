# 🧩 Merge Intervals (Python)

## 📌 Objective
The goal of this project is to **merge overlapping intervals** from a list of intervals.  
Each interval is represented as a pair of integers `[start, end]`.

---

## 📖 Problem Explanation
Given a list of intervals, merge all the overlapping intervals and return a new list of non-overlapping intervals
